# Supports de formation GenAI mai/juin 2025

## Installation

Lorsque l'environnement est démarré 

```bash
poetry install --no-root
```

Ensuite sélectionner le bon noyau :

1. Dans sélectionner le noyau : installer un environnement jupyter
2. Une fois installé, sélectionne l'environnement qui commence par jupyter-

## Utilisation de graphviz 

```bash
sudo apt update
sudo apt install -y graphviz
```
